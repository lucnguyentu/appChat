/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  compiler: {
    styledComponents: true,
  } // add to compatible nextJs to styledComponents 
}

module.exports = nextConfig
